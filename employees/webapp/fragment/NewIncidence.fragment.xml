<c:FragmentDefinition
    xmlns="sap.m"
    xmlns:c="sap.ui.core">
    <Panel>
        <headerToolbar>
            <Toolbar>
                <c:Icon src="{= ${incidenceModel>Type} === '0' ? 'sap-icon://accept' :
                                ${incidenceModel>Type} === '1' ? 'sap-icon://alert' :
                                ${incidenceModel>Type} === '2' ? 'sap-icon://decline' : '' }"></c:Icon>
                <Title text="{i18n>incidence} {= ${incidenceModel>IncidenceId} ? parseInt(${incidenceModel>IncidenceId}) : ''}
                             {path : 'incidenceModel>CreationDate',
                              formatter: '.Formatter.dateFormat'}"/>
                <ToolbarSpacer/>
                <Button icon="sap-icon://save" press="onSaveIncidence"/>
                <c:Icon src="sap-icon://delete"/>
            </Toolbar>
        </headerToolbar>

            <HBox>
                <VBox class="sapUiSmallMarginEnd">
                    <Text text="{i18n>incidenceDate}"/>
                    <DatePicker dateValue="{incidenceModel>CreationDate}" change="updateIncidenceCreationDate"/>
                </VBox>
                <VBox visible="{= ${incidenceModel>CreationDate} !== '' &amp;&amp; ${incidenceModel>CreationDate} !== undefined}" class="sapUiSmallMarginEnd">
                    <Text text="{i18n>incidenceReason}"/>
                    <Input value="{incidenceModel>Reason}" change="updateIncidenceReason"/>
                </VBox>
                <VBox visible="{= ${incidenceModel>CreationDate} !== '' &amp;&amp; ${incidenceModel>CreationDate} !== undefined}">
                    <Text text="{i18n>incidenceType}"/>
                    <Select forceSelection="false" selectedKey="{incidenceModel>Type}" change="updateIncidenceType">
                        <c:Item key="0" text="{i18n>incidenceCorrect}"/>
                        <c:Item key="1" text="{i18n>incidencePending}"/>
                        <c:Item key="2" text="{i18n>incidenceError}"/>
                    </Select>
                </VBox>
            </HBox>

    </Panel>
</c:FragmentDefinition>
